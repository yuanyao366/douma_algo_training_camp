var test = function() {
    const stack = []
    // 压栈
    stack.push(2)
    stack.push(3)

    // 栈顶元素
    let top = stack[stack.length - 1]
    const isEmpty = stack.length == 0

    // 出栈
    const top = stack.pop()
}
